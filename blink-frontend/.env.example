# Where the Blink backend (blink-server) is running. Set this in Vercel only
# after deploying the API to a host with persistent database storage. When it
# is unset in production, Blink keeps account/profile data in this browser.
VITE_API_BASE_URL=http://localhost:3000
